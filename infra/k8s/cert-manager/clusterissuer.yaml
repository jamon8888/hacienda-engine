# cert-manager ClusterIssuer for Let's Encrypt
# Install cert-manager first:
# kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.13.0/cert-manager.yaml

---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: ops@example.com
    privateKeySecretRef:
      name: letsencrypt-staging
    solvers:
      - http01:
          ingress:
            class: nginx
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: ops@example.com
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
      - http01:
          ingress:
            class: nginx
---
# Certificate for hacienda.example.com (auto-created by Ingress)
# This is just for reference - Ingress with cert-manager.io/cluster-issuer annotation creates it
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: hacienda-tls
#   namespace: hacienda-prod
# spec:
#   secretName: hacienda-tls
#   issuerRef:
#     name: letsencrypt-prod
#     kind: ClusterIssuer
#   dnsNames:
#     - hacienda.example.com
#     - '*.hacienda.example.com'
